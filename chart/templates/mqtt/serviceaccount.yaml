{{- if .Values.mqtt.gateway.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mqtt-gateway
  namespace: {{ .Values.mqtt.gateway.service.namespace }}
  labels:
    {{- include "gps-connector.labels" . | nindent 4 }}
{{- end }}
