{{- if .Values.secretData.main }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.main }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gps-connector.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- toYaml .Values.secretData.main | nindent 2 }}
{{- end }}
